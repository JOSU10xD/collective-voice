export default {
    plugins: {
        '@tailwindcss/postcss': {},
        autoprefixer: {}, // Autoprefixer is still useful for some vendor prefixes that tailwind might not handle internally or if using other css
    },
}
